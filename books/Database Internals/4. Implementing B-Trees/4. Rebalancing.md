Split and merge is expensive because you must create new nodes and move data which also increases traversal costs.  
Some implementations may attempt to move keys into siblings if possible and fallback to split and merge.  