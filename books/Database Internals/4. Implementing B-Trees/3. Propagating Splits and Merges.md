While theoretically a child does not need to store a pointer to its parent in real implementations a child may keep a pointer as an optimisation for traversal e.g when the tree needs to be balanced and changes may propagate up the tree.
# Breadcrumbs
Instead of storing parent pointers on disk they can store metadata about the current traversal on a in-memory stack which allows them to propagate back up and update the tree.